record(waveform, "$(P):$(R)") {
  field(DESC, "$(DESC)")
  field(DTYP,	"asynOctetRead")
  field(NELM,	"$(N)")
  field(FTVL,	"CHAR")
  field(SCAN,	"5 second")
  field(PHAS,   "1")
  field(INP,	"@asyn($(PORT),$(ADDR))$(PARAM)")
}

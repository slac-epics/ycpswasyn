record(waveform, "$(P):$(R)") {
  field(DESC, "$(DESC)")
  field(DTYP,	"asynOctetRead")
  field(NELM,	"$(N)")
  field(FTVL,	"CHAR")
  field(SCAN,	"Passive")
  field(PHAS,   "1")
  field(INP,	"@asyn($(PORT),$(ADDR))$(PARAM)")
}
